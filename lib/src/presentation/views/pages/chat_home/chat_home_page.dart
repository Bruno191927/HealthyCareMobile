import 'package:flutter/material.dart';

class ChatHomePage extends StatelessWidget {
  static const routeName = "ChatHomePage";
  const ChatHomePage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}